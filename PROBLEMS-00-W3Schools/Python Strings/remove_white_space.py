character = "Hey python !"
print(character)

print(character.strip("H"))
# The strip() method in Python is used to remove characters (specified as an argument) from the beginning and end of a string.

